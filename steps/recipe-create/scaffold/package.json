{
  "name": "{{ recipeName }}",
  "version": "{{ recipeVersion }}",
  "description": "{{ recipeDescription }}",
  "main": "bin/pisco.js",
  "bin": {
    "{{ recipeCmdName }}": "bin/pisco.js"
  },
  "scripts": {
    "deps": "npm install",
    "test": "node_modules/.bin/mocha"
  },
  "keywords": [
    "piscosour-recipe"
  ],
  "license": "Apache Licence",
  "preferGlobal": true,
  "dependencies": {
    "piscosour": "^1.0.0"
  },
  "devDependencies": {
    "chai": "*",
    "mocha": "*"
  },
  "engines": {
    "node": ">=4.0.0"
  }
}
